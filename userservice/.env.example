# Shared secret used to verify requests actually came from the gateway.
# Must match INTERNAL_API_SECRET in gateway, activityservice, and aiservice.
INTERNAL_API_SECRET=change-me

# PostgreSQL connection (defaults below match a typical local install)
POSTGRES_URL=jdbc:postgresql://localhost:5432/fitness_user_db
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=change-me
