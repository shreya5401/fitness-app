# MongoDB connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/activ?appName=<app-name>

# Shared secret used to verify requests actually came from the gateway.
# Must match INTERNAL_API_SECRET in gateway, userservice, and aiservice.
INTERNAL_API_SECRET=change-me
